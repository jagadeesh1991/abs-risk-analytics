# Normalize line endings so scripts work when the repo is cloned on any OS.
* text=auto
*.sh text eol=lf
*.ps1 text eol=crlf
*.png binary
*.svg text
*.parquet binary
data/** filter=lfs diff=lfs merge=lfs -text
